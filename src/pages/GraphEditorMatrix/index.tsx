import React from 'react';
import GraphEditor from '../GraphEditor';

// GraphEditorMatrix = GraphEditor with the adjacency matrix panel enabled
const GraphEditorMatrix = () => <GraphEditor withMatrix />;

export default GraphEditorMatrix;
